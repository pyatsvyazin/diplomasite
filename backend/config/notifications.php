<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Email для in-app уведомлений (через Laravel Mail, те же MAIL_* что и для кодов)
    |--------------------------------------------------------------------------
    |
    | Статус заявки, консультации, напоминания о консультации — дублируют колокольчик.
    | Письма регистрации / 2FA / пароля не зависят от этого флага (всегда через AuthController).
    |
    */
    'mail_enabled' => (bool) env('NOTIFICATIONS_MAIL_ENABLED', false),

];
